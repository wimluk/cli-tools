{
  "name": "@cli-tools/{{name}}",
  "version": "0.1.0",
  "description": "{{description}}",
  "private": true,
  "scripts": {
    "build": "echo 'No build step for Python'",
    "build:python": "echo 'No build step for Python'",
    "dev": "python -m {{name}}.cli",
    "lint": "ruff check .",
    "lint:python": "ruff check .",
    "format": "ruff format .",
    "test": "pytest",
    "clean": "find . -type d -name __pycache__ -exec rm -rf {} + 2>/dev/null || true"
  }
}